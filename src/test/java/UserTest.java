import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class UserTest {

   /* @Test
    public void testCreateSchedule() {
        User user011 = new User("test", "password");
        User user101 = new User("test", "password");
        User user110 = new User("test", "password");
        User user111 = new User("test", "password");

        user011.setAvailability();
        user011.createSchedule();

        user101.login();
        user101.setAvailability();

        user110.login();
        user110.createSchedule();

        user111.login();
        user111.setAvailability();
        user111.createSchedule();

        assertEquals(user011.checklist(), 2);
        assertEquals(user101.checklist(), 2);
        assertEquals(user110.checklist(), 2);
        assertEquals(user111.checklist(), 3);
    }
*/
}